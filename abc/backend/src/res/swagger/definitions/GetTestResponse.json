{
    "type": "object",
    "required": [
        "success",
        "data"
    ],
    "properties": {
        "success": {
            "description": "Indicates, if operation was successful or not.",
            "type": "boolean",
            "example": "true"
        },
        "data": {
            "type": "object",
            "required": [
                "isSqlConnectionWorking",
                "message"
            ],
            "properties": {
                "isMongoConnectionWorking": {
                    "description": "Indicates if Mongo connection works or not.",
                    "type": "boolean",
                    "example": true
                },
                "isSqlConnectionWorking": {
                    "description": "Indicates if SQL connection works or not.",
                    "type": "boolean",
                    "example": true
                },
                "message": {
                    "description": "The message.",
                    "type": "string",
                    "example": "Hello, e.GO!"
                }
            }
        }
    }
}