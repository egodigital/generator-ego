{
    "type": "object",
    "required": [
        "success",
        "data"
    ],
    "properties": {
        "success": {
            "description": "Indicates, if operation was successful or not.",
            "type": "boolean",
            "example": "true"
        },
        "data": {
            "type": "object",
            "required": [
                "message"
            ],
            "properties": {
                "message": {
                    "description": "The message.",
                    "type": "string",
                    "example": "Hello, e.GO!"
                }
            }
        }
    }
}