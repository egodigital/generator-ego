{
    "type": "object",
    "required": [
        "foo"
    ],
    "properties": {
        "foo": {
            "description": "Some value.",
            "type": "string",
            "example": "bar",
            "enum": [
                "bar",
                "baz"
            ]
        }
    }
}