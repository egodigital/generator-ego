{
    "type": "object",
    "required": [
        "success",
        "message"
    ],
    "properties": {
        "success": {
            "description": "Indicates, if operation was successful or not.",
            "type": "boolean",
            "example": "true"
        },
        "message": {
            "description": "The message.",
            "type": "string",
            "example": "Your foo: bar"
        }
    }
}